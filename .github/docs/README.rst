.github/docs (Deprecated)
========================

This folder is kept for backward compatibility.

**Use templates from**: `.github/templates/`.

If you maintain older automation that still reads `.github/docs/templates/`, you may keep it.
Otherwise, prefer the canonical templates under `.github/templates/`.
