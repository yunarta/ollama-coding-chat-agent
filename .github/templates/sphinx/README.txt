Sphinx scaffold template

Copy the entire `docs/` folder from this template into repo root `docs/`.
Then build with:
  pip install -r docs/requirements.txt
  sphinx-build -b html docs docs/_build/html
